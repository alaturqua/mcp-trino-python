# Trino Connection Settings
TRINO_HOST=localhost
TRINO_PORT=8080
TRINO_USER=trino
TRINO_CATALOG=tpch
TRINO_SCHEMA=tiny
TRINO_HTTP_SCHEME=http
# TRINO_PASSWORD= # Optional: Set password if authentication is required

# Optional: Set default catalog and schema
# TRINO_CATALOG=iceberg
# TRINO_SCHEMA=default
